import { ExpirationtDateFormatPipe } from './expirationt-date-format.pipe';

describe('ExpirationtDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpirationtDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
